
document.querySelectorAll(".entity-input").forEach(input => {
    input.classList.add("disable");
});
//document.querySelectorAll("input").forEach(input => {
//    input.classList.add("disable");
//});
//document.querySelectorAll("textarea").forEach(input => {
//    input.classList.add("disable");
//});
//document.querySelectorAll("select").forEach(input => {
//    input.classList.add("disable");
//});


    //if (imgInput.files.length > 0) { // Check if a file is already selected
    //    const file = imgInput.files[0];
    //    const reader = new FileReader();

    //    reader.onload = function (e) {
    //        img.src = e.target.result; // Set image source
    //        img.style.display = "block";
    //        label.style.display = "none";
    //    };

    //    reader.readAsDataURL(file);
    //}
    //else {
    //    img.src = "https://placehold.co/500x600/png";
    //    img.alt = "There is no image";
    //    img.style.display = "block";
    //    label.style.display = "none";

    //}
